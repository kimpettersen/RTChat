/*
var App = Ember.Application.create();

App.Message = Ember.object.extend{message : ''};

App.currentMessage = App.Message.create();

App.MessageController = Ember.ArrayProxy.create({
   //used to send messages via server
   funcname : function(){}; 
});
*/